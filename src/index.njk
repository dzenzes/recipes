---
layout: layouts/base.njk
eleventyNavigation:
  key: Home
  order: 1
---

<p>
  Ich teste immer wieder gerne neue Rezepte und habe hier einige davon gesammelt um überall darauf zugreifen zu können. 🧑‍🍳
</p>

<ul class="postlist">
  {% for recipe in collections.recipes %}
    <li
    class="postlist-item{% if recipe.url == url %} postlist-item-active{% endif %}"
  >
      <a href="{{ recipe.url | url }}" class="postlist-link">
        {% if recipe.data.title %}
          {{ recipe.data.title }}
        {% else %}
          <code>{{ recipe.url }}</code>
        {% endif %}
      </a>
      {#
&nbsp;
      ({% for tag in recipe.data.tags %}
        {%- if tag != "posts" -%}
          <a href="{{ tag | tagUrl('recipe' )| url }}">#{{ tag | formatTag }}</a>
        {%- endif -%}
        {%- if not loop.last %}, {% endif %}
      {% endfor %})
#}
    </li>
  {% endfor %}
</ul>